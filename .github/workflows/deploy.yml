name: Deploy fintrack-bot

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run deploy script
        run: /bin/bash /home/dul/scripts/deploy-fintrack-bot.sh
